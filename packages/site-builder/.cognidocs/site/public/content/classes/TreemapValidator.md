# TreemapValidator

Implementation of custom validations.

## Methods

### checkSingleRoot

Validates that a treemap has only one root node. A root node is defined as a node that has no indentation.

**Return:** `void`
