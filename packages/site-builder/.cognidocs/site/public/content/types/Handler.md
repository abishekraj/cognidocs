# Handler

Handle a value, with a certain ID field set to a certain value. The ID field is passed to `zwitch`, and it’s value is this function’s place on the `handlers` record.

## Definition

```typescript
(...parameters: any[]) => any;
```
