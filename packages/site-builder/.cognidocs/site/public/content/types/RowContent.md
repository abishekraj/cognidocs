# RowContent

Union of registered mdast nodes that can occur where row content is expected. To register custom mdast nodes, add them to {@link RowContentMap}. They will be automatically added here.

## Definition

```typescript
RowContentMap[keyof RowContentMap]
```
