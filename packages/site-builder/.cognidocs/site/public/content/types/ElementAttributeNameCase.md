# ElementAttributeNameCase

Casing to use for attribute names. HTML casing is for example `class`, `stroke-linecap`, `xml:lang`. React casing is for example `className`, `strokeLinecap`, `xmlLang`.

## Definition
```typescript
'html' | 'react'
```