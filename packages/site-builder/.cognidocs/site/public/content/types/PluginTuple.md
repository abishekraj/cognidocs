# PluginTuple

Tuple of a plugin and its configuration. The first item is a plugin, the rest are its parameters.

## Definition

```typescript
([
    plugin: Plugin<TupleParameters, Input, Output>,
    ...parameters: TupleParameters
])
```
