# ListContent

Union of registered mdast nodes that can occur where list content is expected. To register custom mdast nodes, add them to {@link ListContentMap}. They will be automatically added here.

## Definition

```typescript
ListContentMap[keyof ListContentMap]
```
