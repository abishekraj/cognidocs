# ConstructName

Construct names for things generated by `mdast-util-to-markdown`. This is an enum of strings, each being a semantic label, useful to know when serializing whether we’re for example in a double (`"`) or single (`'`) quoted title.

## Definition
```typescript
ConstructNameMap[keyof ConstructNameMap]
```