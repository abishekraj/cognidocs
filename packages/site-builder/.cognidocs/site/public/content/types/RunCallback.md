# RunCallback

Callback called when transformers are done. Called with either an error or results.

## Definition
```typescript
(error?: Error | undefined, tree?: Tree | undefined, file?: VFile | undefined) => undefined
```