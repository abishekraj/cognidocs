# StylePropertyNameCase

Casing to use for property names in `style` objects. CSS casing is for example `background-color` and `-webkit-line-clamp`. DOM casing is for example `backgroundColor` and `WebkitLineClamp`.

## Definition

```typescript
'css' | 'dom';
```
