# RootContent

Union of registered mdast nodes that can occur in {@link Root}. To register custom mdast nodes, add them to {@link RootContentMap}. They will be automatically added here.

## Definition

```typescript
RootContentMap[keyof RootContentMap]
```
