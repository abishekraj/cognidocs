# toDecodedMap

Returns a sourcemap object (with decoded mappings) suitable for passing to a library that expects a sourcemap, or to JSON.stringify.

**Return Type:** `DecodedSourceMap`

## Parameters

| Name  | Type         | Optional | Description |
| :---- | :----------- | :------- | :---------- |
| `map` | `GenMapping` | No       | -           |
