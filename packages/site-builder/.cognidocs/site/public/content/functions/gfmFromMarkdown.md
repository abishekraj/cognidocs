# gfmFromMarkdown

Create an extension for `mdast-util-from-markdown` to enable GFM (autolink literals, footnotes, strikethrough, tables, tasklists).


**Return Type:** `Array<FromMarkdownExtension>`
