{
  "name": "@cognidocs/analyzer",
  "version": "0.1.0",
  "description": "Code analysis and dependency graph generation - Phase 2",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cognidocs/parser": "*",
    "@cognidocs/types": "*",
    "@cognidocs/utils": "*"
  },
  "devDependencies": {
    "tsup": "^8.0.1",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "keywords": ["analyzer", "dependencies", "graph"],
  "license": "MIT"
}
